class Solution {
    public int findKthLargest(int[] nums, int k) {
        
        Arrays.sort(nums); // sorts the elements 
        return nums[nums.length - k]; // indexs the elemen minus the total length
        
        // sort the elements 
        // index the kth element
        
        // could use a heap 
        
 
        
    }
}
